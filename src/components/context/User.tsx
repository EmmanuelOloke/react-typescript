export const User = () => {
  const handleLogin = () => {
    // some code here
  };
  const handleLogout = () => {
    // some code here
  };
  return (
    <div>
      <button onClick={handleLogin}></button>
      <button onClick={handleLogout}></button>
      <div>User name is</div>
    </div>
  );
};
